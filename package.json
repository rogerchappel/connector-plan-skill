{
  "name": "connector-plan-skill",
  "version": "0.1.0",
  "description": "Local-first connector routing and dry-run planning for agent workflows.",
  "type": "module",
  "bin": {
    "connector-plan": "./src/cli.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/*.js test/*.test.js",
    "smoke": "node src/cli.js route --task fixtures/follow-up-task.json --connectors fixtures/connectors --policy fixtures/policy.json --format markdown",
    "package:smoke": "npm pack --dry-run",
    "release:check": "npm run check && npm test && npm run smoke && npm run package:smoke"
  },
  "keywords": [
    "agent-skill",
    "connectors",
    "dry-run",
    "planning"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "files": [
    "src",
    "fixtures",
    "docs",
    "SKILL.md",
    "README.md",
    "LICENSE"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rogerchappel/connector-plan-skill.git"
  },
  "bugs": {
    "url": "https://github.com/rogerchappel/connector-plan-skill/issues"
  },
  "homepage": "https://github.com/rogerchappel/connector-plan-skill#readme"
}
